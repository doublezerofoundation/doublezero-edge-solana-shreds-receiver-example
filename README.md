# doublezero-edge-solana-shreds-receiver-example
Example receipt of shreds over doublezero-edge-solana. The program has an example: main() that counts shreds and prints throughput every 10,000 packets. This is a smoke test stand in for any strategy which would receive the shreds. It also provides basic deduplication. Although the user should remove this, and deduplicate downstream.
